name: Deploy to Portainer

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    uses: huskies-pro/.github/.github/workflows/reusable-portainer-deploy.yml@main
    secrets:
      webhook_url: ${{ secrets.PORTAINER_WEBHOOK_URL }}
